# Backlog

Known-but-unspecced capabilities and gaps. One line each; promote to a
numbered spec when work starts.

- Mouse input and mouse-driven session selection (observed unknown, spec 003/004).
- Image/path clipboard paste (observed unknown, spec 003).
- Successful-clipboard provider discovery beyond OSC52/xclip (spec 003).
- History clearing, malformed history files, concurrent writers (spec 004).
- Session creation/switching/refresh internals beyond the observed slice (spec 004).
- Provider OAuth, remote/gateway providers, credential storage (spec 005/006).
- Tool execution (tool-call deltas are rendered, not executed) (spec 005).
- Gateway behavior and concurrent input during overlays (spec 007).
- Performance and resource budgets (roadmap Stage 4, spec 001).
- Supported environment matrix and reproducible packaging (roadmap Stage 5, spec 001).
- Configured-state Ctrl+C echo semantics confirmation (open question, spec 002).
- Verify orchestration retirement design (HAD-178) (spec 009).
